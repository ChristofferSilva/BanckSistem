package projeto;

//import java.time.LocalDate;
//import java.time.format.DateTimeFormatter;
//import java.util.ArrayList;
//import java.util.List;

// import static jdk.internal.org.jline.utils.InfoCmp.Capability.columns;
//
//public class BanckStatementeCsvParcer {
  //  private static final DateTimeFormatter formatter = DateTimeFormatter.ofPattern("dd/MM/yyyy");
    
   // private BanckTransaction parseFromCSV(final String line){
     //   final String[] fields = line.split(",");
        
      //  final LocalDate date = LocalDate.parse(columns[0]), DATE_PATTERNS);
       // final double amount = Double .parseDouble(columns[1]);
       // final String description = columns[2];
        
       // return new BanckTransaction> parseLinesFromCSV(final List < String > lines) {
         //   final List<BanckTransaction> transactions = new ArrayList<>();
           // for (final String Lines : lines) {
             //   BanckTransaction.add(parseFromCSV(line));
           // }
           // return BanckTransaction;
      //  }

   // }
// }
//
// Classes de dominio que representa um extrato bancario.